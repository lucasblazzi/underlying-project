@startuml
!theme aws-orange
!include https://raw.githubusercontent.com/kirchsth/C4-PlantUML/extended/C4_Container.puml

title visão de deployment

cloud cloud{

node frontend{
    Container(web_app, "Aplicação Web","Underlying")
    Container(api, "API")
}

node autenticação{
    Container(auth,"Autenticador")
}

node Serverless{
    Container(lambdas,"Serverless Functions")
    Container(user_lambdas,"User Functions","Dados referentes aos Clientes")
}

node Banco_de_dados{
    ContainerDb(banco,"Banco de dados")
}
}

node Banco_externo {
    System_Ext(b3,"Dados da B3")
}

BiRel_R(frontend, autenticação," ")
BiRel(autenticação, Serverless," ")
BiRel(Serverless, Banco_de_dados," ")
BiRel_R(Serverless, Banco_externo, " ")


@enduml